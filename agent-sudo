#!/usr/bin/env bash
set -euo pipefail

if [ "$#" -eq 0 ]; then
    echo "Usage: agent-sudo <command> [args...]" >&2
    exit 2
fi

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
export SUDO_ASKPASS="$SCRIPT_DIR/askpass"

exec sudo -A "$@"
